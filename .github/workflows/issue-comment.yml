name: gif comment
on: 
  issues:
    types: [opened]
    
jobs:
  comment:
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps:
      - name: Add funny comment
        uses: Marta83/funny-comment-action@v2.1
        id: FunnyComment
        with:
          GITHUB_TOKEN: ${{ secrets.85e59d2aa1a148d8d6af5e36b62906218dfe07a7 }}
          GIPHY_TOKEN: ${{ secrets.2p2Y5pvMDxr4l84myTcbdRoTxCYOl4NA }}
          comment_type: 'gif'
